export enum StyleConstants {
  NAV_BAR_WIDTH = 50,
  NAV_BAR_HEIGHT_PX = '60px',

  TRANSITION_SPEED = 300,
  TRANSITION_SPEED_MS = '300ms',

  GLOBAL_PADDING = 16,
  GLOBAL_PADDING_PX = '16px',
}

export const zIndex = Object.freeze({
  zero: 0,

  modalBackdrop: 99,
  modal: 100,
});
